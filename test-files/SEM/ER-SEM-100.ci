@cep(21, "variable 'error' not found", "", "")

class Teste
    var Int n;
	
    func get -> Int {
        return self.n;
    }
	
    func set: Int n {
        self.n = n;
    }
end

class Program
    func run {
        var Teste t;
        var Int a;
        t = Teste.new;
        a = In.readInt;
        t.set: error;
        Out.println: t.get;
    }
end
