@cep(9, "An overridden method should be preceded by 'override'", "override missing", "")

open 
class A
    public func nothing {
    }
end

class B extends A
    public func nothing {
    }
end

class Program 
    func run {
    }
end
