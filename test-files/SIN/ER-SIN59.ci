/* 
Luciane Lopes
luciane.silva@msn.com
*/

@cep(12, "'until' ausente", 
  "Statement expected", 
  "comp.Compiler.repeatStat()")

class Program

    func run {
        var Int i;
        i = 0;
        repeat
            i = i + 1;
        (i < 10);
    }

end
