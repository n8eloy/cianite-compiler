@cep(9, "falta ';' após a atribuição", "';' missing")

class Program
  
    func run {

        var Int i;
        In.readInt(i);
        if 3 > 0 {
            i = 1        //#
        }
    }
end
