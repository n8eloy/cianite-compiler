@cep(9, "falta ';' apos a atribuicao", "';' missing")

class Program

    func run {

        var Int i;
        i = In.readInt;
        if 3 > 0 {
            i = 1        //#
        }
    }
end